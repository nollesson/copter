major_version: "1"
minor_version: "0"
default_target_cpu: "avr"

default_toolchain {
  cpu: "avr"
  toolchain_identifier: "avr8-gnu-toolchain-linux_x86_64"
}

toolchain {
  toolchain_identifier: "avr8-gnu-toolchain-linux_x86_64"

  host_system_name: "x86_64-pc-linux-gnu"
  target_system_name: "avr"
  target_cpu: "avr"
  target_libc: "avr"
  compiler: "gcc"

  abi_version: "avr"
  abi_libc_version: "avr"

  tool_path {
    name: "ar"
    path: "/usr/bin/avr-ar"
  }
  tool_path {
    name: "compat-ld"
    path: "/usr/bin/avr-ld"
  }
  tool_path {
    name: "cpp"
    path: "/usr/bin/avr-cpp"
  }
  tool_path {
    name: "gcc"
    path: "/usr/bin/avr-gcc"
  }
  tool_path {
    name: "gcov"
    path: "/usr/bin/avr-gcov"
  }
  tool_path {
    name: "ld"
    path: "/usr/bin/avr-ld"
  }
  tool_path {
    name: "nm"
    path: "/usr/bin/avr-gcc-nm"
  }
  tool_path {
    name: "objcopy"
    path: "/usr/bin/avr-objcopy"
  }
  tool_path {
    name: "objdump"
    path: "/usr/bin/avr-objdump"
  }
  tool_path {
    name: "strip"
    path: "/usr/bin/avr-strip"
  }

  supports_thin_archives: true
  supports_normalizing_ar: true

  # Always include debug info since it has no impact program size when flashed
  compiler_flag: "-g3"

  # Enable coloring even if there's no attached terminal. Bazel removes the
  # escape sequences if --nocolor is specified.
  compiler_flag: "-fdiagnostics-color=always"

  # Warnings
  compiler_flag: "-Wall"
  compiler_flag: "-Wextra"
  compiler_flag: "-pedantic"
  compiler_flag: "-Werror"

  compiler_flag: "-std=c++0x"

  # Make C++ compilation deterministic
  unfiltered_cxx_flag: "-Wno-builtin-macro-redefined"
  unfiltered_cxx_flag: "-D__DATE__=\"redacted\""
  unfiltered_cxx_flag: "-D__TIMESTAMP__=\"redacted\""
  unfiltered_cxx_flag: "-D__TIME__=\"redacted\""

  # Stamp the binary with a unique identifier
  linker_flag: "-Wl,--build-id=md5"

  # Arduino-specific defines
  compiler_flag: "-DF_CPU=16000000"
  compiler_flag: "-mmcu=atmega328p"

  objcopy_embed_flag: "-I"
  objcopy_embed_flag: "binary"

  cxx_builtin_include_directory: "/usr/lib/gcc/avr/4.9.2/include"
  cxx_builtin_include_directory: "/usr/lib/gcc/avr/4.9.2/avr/4.9.2/include-fixed"
  cxx_builtin_include_directory: "/usr/lib/avr/include"

  compilation_mode_flags {
    mode: FASTBUILD
    compiler_flag: "-O0"
  }

  compilation_mode_flags {
    mode: DBG
    compiler_flag: "-O0"
  }

  compilation_mode_flags {
    mode: OPT
    compiler_flag: "-Os"
    compiler_flag: "-DNDEBUG"
    compiler_flag: "-funsigned-bitfields"
    compiler_flag: "-funsigned-char"
    compiler_flag: "-fshort-enums"
    compiler_flag: "-fpack-struct"
  }
}
